---
import MainLayout from "@layouts/MainLayout.astro";
import ProjetDetailHeader from "@/components/projets/Projet_detail_header.astro";
import SocialLink from "@/components/Social_link.astro";
import FloraccessContent from "@components/projets/contents/floraccess_content.astro";
import { getProjectById } from "@/data/projects";

const project = getProjectById("floraccess");

if (!project) {
  throw new Error("Project 'floraccess' not found in projects data.");
}
---

<MainLayout>
  <ProjetDetailHeader
    id={project.id}
    title={project.title}
    description={project.description}
    githubButtonText="Voir le projet sur Github"
    githubButtonLink={project.githubLink}
    externalButtonText="Voir le projet en ligne"
    externalButtonLink={project.projectLink}
  />

  <SocialLink />

  <div class="max-w-7xl mx-auto mb-24">
    <FloraccessContent />
  </div>
</MainLayout>
