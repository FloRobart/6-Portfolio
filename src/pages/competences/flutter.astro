---
import CompetenceDetailHeader from "@/components/competences/Competence_detail_header.astro";
import FlutterContent from "@/components/competences/contents/flutter_content.astro";
import MainLayout from "@layouts/MainLayout.astro";
import { getCompetenceById } from "@/data/competences";
import SocialLink from "@/components/Social_link.astro";

const competence = getCompetenceById("flutter");

if (!competence) {
  throw new Error("Competence 'flutter' not found in competences data.");
}
---

<MainLayout>
  <CompetenceDetailHeader
    id={competence.id}
    title={competence.title}
    description={competence.description}
  />

  <SocialLink />

  <div class="max-w-7xl mx-auto mb-24">
    <FlutterContent />
  </div>
</MainLayout>
